pub struct SSTwoWay{
    rows: f64,
    cols: f64,
    error: f64,
    sum: f64
}
pub struct DFTwoWay{
    rows: usize,
    cols: usize,
    error: usize,
    sum: usize
}
pub struct MSTwoWay{
    rows: f64,
    cols: f64,
    error: f64
}
pub struct FTwoWay{
    rows: f64,
    cols: f64
}
pub struct PTwoWay{
    rows: f64,
    cols: f64
}
pub struct FCritTwoWay{
    rows: f64,
    cols: f64
}
pub struct SSOneWay{
    inside: f64,
    between: f64,
    sum: f64
}
pub struct DFOneWay{
    inside: usize,
    between: usize,
    sum: usize
}
pub struct MSOneWay{
    inside: f64,
    between: f64
}
pub struct FOneWay{
    inside: f64,
    beetwen: f64
}
